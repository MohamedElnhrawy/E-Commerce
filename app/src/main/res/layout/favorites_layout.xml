<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <variable
            name="viewModel"
            type="com.gtera.ui.profile.favorites.FavoritesViewModel" />

    </data>


    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="0dp"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/tab_layout"
                app:layout_constraintVertical_bias="0">

                <ViewStub
                    android:id="@+id/loadingView"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:layout="@layout/base_loading_layout" />

                <ViewStub
                    android:id="@+id/emptyView"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:layout="@layout/base_empty_layout" />


                <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
                    android:id="@+id/swipeRef"
                    isRefreshing="@{viewModel.isRefreshing}"
                    refreshListener="@{viewModel.onRefreshListener}"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent">

                    <androidx.constraintlayout.widget.ConstraintLayout
                        android:layout_width="match_parent"
                        android:layout_height="match_parent">

                        <androidx.recyclerview.widget.RecyclerView
                            android:id="@+id/rv_new_cars_favorites"
                            adapter="@{viewModel.newCarsFavoritesAdapter}"
                            recyclerSwipe="@{swipeRef}"
                            viewOrientation="@{viewModel.newCarsFavoritesOrientation}"
                            visibleIf="@{viewModel.hasNewCarFavorite}"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            app:layout_constraintEnd_toEndOf="parent"
                            app:layout_constraintStart_toStartOf="parent"
                            app:layout_constraintTop_toTopOf="parent"
                            tools:itemCount="2"
                            tools:listitem="@layout/car_item_list_layout" />

                        <androidx.recyclerview.widget.RecyclerView
                            android:id="@+id/rv_used_cars_favorites"
                            adapter="@{viewModel.usedCarsFavoritesAdapter}"
                            recyclerSwipe="@{swipeRef}"
                            viewOrientation="@{viewModel.usedCarsFavoritesOrientation}"
                            visibleIf="@{viewModel.hasUsedCarFavorite}"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            app:layout_constraintEnd_toEndOf="parent"
                            app:layout_constraintStart_toStartOf="parent"
                            app:layout_constraintTop_toTopOf="parent"
                            tools:itemCount="2"
                            tools:listitem="@layout/car_item_list_layout" />

                        <androidx.constraintlayout.widget.ConstraintLayout
                            visibleIf="@{viewModel.hasEmptyFavorite}"
                            android:layout_width="match_parent"
                            android:layout_height="match_parent"
                            android:background="@color/empt_view_background_color"
                            app:layout_constraintEnd_toEndOf="parent"
                            app:layout_constraintStart_toStartOf="parent"
                            app:layout_constraintTop_toTopOf="parent">

                            <ImageView
                                android:id="@+id/iv_empty"
                                android:layout_width="0dp"
                                android:layout_height="0dp"
                                android:padding="@dimen/dim_base_empty_layout_iv_empty_padding"
                                app:layout_constraintBottom_toBottomOf="@id/tv_empty_title"
                                app:layout_constraintBottom_toTopOf="@id/btn_empty"
                                app:layout_constraintDimensionRatio="1:1"
                                app:layout_constraintEnd_toEndOf="parent"
                                app:layout_constraintStart_toStartOf="parent"
                                app:layout_constraintTop_toTopOf="parent"
                                app:layout_constraintVertical_bias="0.42"
                                app:layout_constraintWidth_percent="0.25"
                                app:srcCompat="@drawable/ic_favorite_empty" />

                            <TextView
                                android:id="@+id/tv_empty_title"
                                style="@style/AppTheme.TextStyle.EmptyView.Header"
                                android:layout_width="0dp"
                                android:layout_height="wrap_content"
                                android:layout_marginBottom="@dimen/_10sdp"
                                android:gravity="center"
                                android:text="@string/str_empty_favorite_header"
                                app:layout_constraintBottom_toTopOf="@id/iv_empty"
                                app:layout_constraintEnd_toEndOf="parent"
                                app:layout_constraintStart_toStartOf="parent"
                                app:layout_constraintVertical_bias="0.2"
                                app:layout_constraintWidth_percent="0.7"
                                tools:text="@string/str_empty_favorite_header" />

                            <TextView
                                android:id="@+id/tv_empty_desc"
                                style="@style/AppTheme.TextStyle.EmptyView.Description"
                                android:layout_width="0dp"
                                android:layout_height="wrap_content"
                                android:layout_marginTop="@dimen/_10sdp"
                                android:gravity="center"
                                android:text="@string/str_empty_favorite_message"
                                app:layout_constraintBottom_toTopOf="@id/btn_empty"
                                app:layout_constraintEnd_toEndOf="parent"
                                app:layout_constraintStart_toStartOf="parent"
                                app:layout_constraintTop_toBottomOf="@id/iv_empty"
                                app:layout_constraintVertical_bias="0.02"
                                app:layout_constraintWidth_percent="0.7"
                                tools:text="@string/str_empty_favorite_message" />

                            <Button
                                android:id="@+id/btn_empty"
                                android:layout_width="0dp"
                                android:layout_height="wrap_content"
                                android:layout_marginBottom="@dimen/_8sdp"
                                android:background="@drawable/rounded_corners_primary"
                                android:onClick="@{() -> viewModel.addToFavorite()}"
                                android:text="@string/str_add"
                                android:textAllCaps="false"
                                android:textColor="@color/buttonTextColor"
                                app:layout_constraintBottom_toBottomOf="parent"
                                app:layout_constraintEnd_toEndOf="parent"
                                app:layout_constraintStart_toStartOf="parent"
                                app:layout_constraintWidth_percent="0.94"
                                tools:text="Start add" />


                        </androidx.constraintlayout.widget.ConstraintLayout>

                    </androidx.constraintlayout.widget.ConstraintLayout>


                </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

            </FrameLayout>

            <com.google.android.material.tabs.TabLayout
                android:id="@+id/tab_layout"
                style="@style/TabsStyle"
                currentPos="@{viewModel.currentTabPos}"
                tabAdapter="@{viewModel.tabList}"
                tabListener="@{viewModel.tabListener}"
                visibleIf="@{viewModel.tabList.size()>0}"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:elevation="@dimen/_3sdp"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:tabIndicatorHeight="@dimen/_3sdp" />


        </androidx.constraintlayout.widget.ConstraintLayout>


    </androidx.coordinatorlayout.widget.CoordinatorLayout>

</layout>