<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <variable
            name="viewModel"
            type="com.gtera.ui.home.viewmodels.NewsItemViewModel" />

    </data>

    <androidx.cardview.widget.CardView
        app:cardCornerRadius="@dimen/_4sdp"
        android:layout_width="@dimen/_180sdp"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/_3sdp"
        app:cardElevation="@dimen/_1sdp"
        app:cardUseCompatPadding="true">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="@dimen/_4sdp">

            <ImageView
                android:id="@+id/iv_news_image"
                imageUrl="@{viewModel.imageUrl}"
                roundRadius="@{20}"
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:background="@drawable/top_rounded_corners_white_bg"
                android:scaleType="fitCenter"
                app:layout_constraintWidth_percent=".95"
                app:layout_constraintDimensionRatio="2:1"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintBottom_toTopOf="@id/tv_news_name"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:srcCompat="@mipmap/ic_logo" />

            <TextView
                android:id="@+id/tv_news_name"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/_2sdp"
                android:autoMirrored="true"
                android:lines="2"
                android:maxLines="2"
                android:text="@{viewModel.newsTitle}"
                android:textAlignment="viewStart"
                android:textDirection="locale"
                app:layout_constraintEnd_toEndOf="@+id/iv_news_image"
                app:layout_constraintStart_toStartOf="@+id/iv_news_image"
                app:layout_constraintTop_toBottomOf="@+id/iv_news_image"
                tools:text="Mercedes-Benz automobile production company....." />
        </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.cardview.widget.CardView>
</layout>
